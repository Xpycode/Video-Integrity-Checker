import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("VideoAnalyzer")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    ContentView()
}
