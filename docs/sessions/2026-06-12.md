# Session: 2026-06-12

## Goal
Set up the project on a new Mac via `/setup` from the GitHub remote, and apply a project-scoped MCP profile.

## Context
- Previous session: [2026-02-27](2026-02-27.md) — reorg, GitHub, code-review fixes, rename, UI polish
- Current phase: polish (~97%)
- This Mac (`1-macOS` workspace) had only an empty `VideoIntegrityChecker/` placeholder folder; the full project lived on GitHub.

## Progress

### Completed
- [x] Ran project detection — repo already ships with Directions installed, so `/setup` was a **clone-and-resume**, not a fresh scaffold
- [x] Cloned `Xpycode/Video-Integrity-Checker` → `VideoIntegrityChecker/` (clean, tracking `origin/main`)
- [x] Applied **swift-macos** MCP profile: wrote `.mcp.json` + pre-approved servers in `.claude/settings.local.json` (swiftlens, XcodeBuildMCP, sosumi, apple-docs, apple-hig, sequential-thinking, vestige)
- [x] Untracked accidentally-committed 20 MB `.fastembed_cache/` (`git rm -r --cached`); already covered by `.gitignore` line 64
- [x] Committed cleanup + `.mcp.json` on branch `chore/untrack-cache` (`e717029`), pushed to origin

### Decisions Made
- Did not history-rewrite the 20 MB cache out of past commits — only stopped tracking it going forward (full purge wasn't requested)
- MCP profile committed to repo (`.mcp.json` is shared); `settings.local.json` stays local/gitignored

## Notes
- The cache stayed tracked despite a matching `.gitignore` rule because `.gitignore` never affects already-committed files — needed an explicit `git rm --cached`
- Gotcha for later: the global `/mcp-profile` command points at a stale template path (`/Users/sim/XcodeProjects/...`, profile `swift`); the real templates are under `/Users/sim/ProgrammingProjects/0-DIRECTIONS/__DIRECTIONS/mcp-templates/` named `swift-macos`
- MCP profile needs a `/clear` (new session) to take effect

## Next
- Open PR for `chore/untrack-cache` and merge
- `/clear` to activate the swift-macos MCP profile
- Resume polish phase: real corrupt/MXF test files, dual-engine cross-reference, UI refinements (toolbar label visibility, app icon)
